server:
  port: 0

spring:
  application:
    name: transferegov-service-test
  datasource:
    url: jdbc:h2:mem:transferegovdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;INIT=CREATE SCHEMA IF NOT EXISTS transferegov_schema
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: transferegov_schema
        format_sql: false
    show-sql: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false

govflow:
  transferegov:
    siconv:
      base-url: http://localhost:8089/downloads/dadosgov
      uf-filter: PB
      connect-timeout-ms: 2000
      read-timeout-ms: 5000
      cron: "-"
      batch-size: 50
    especiais:
      base-url: http://localhost:8089/especiais
      uf-filter: PB
      connect-timeout-ms: 1000
      read-timeout-ms: 2000
      page-size: 10
      max-pages-per-sync: 5
      rate-limit-delay-ms: 0
      max-retries: 2
      cron: "-"
      scheduler-enabled: false
    rabbitmq:
      exchange: govflow.events
      routing-key-alerta-prazo: transferegov.prazo.alerta
      routing-key-alerta-adpf854: transferegov.emenda.adpf854.alerta

logging:
  level:
    root: WARN
    br.com.govflow.transferegov: DEBUG
